<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Checked state (ON) -->
    <item android:color="@color/purpleLight" android:state_checked="true"/>
    <!-- Unchecked state (OFF) -->
    <item android:color="@color/purpleLight" android:state_checked="false"/>
    <item android:color="@color/red_soft" android:state_pressed="true" />
</selector>